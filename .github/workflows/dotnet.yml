name: .NET

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Setup publish profile
      run: printf "${{ secrets.WEB_DEPLOY_PUBLISH_PROFILE }}" >> Properties\publish.pubxml
    - name: Test
      run: ls
    - name: Publish
      run: dotnet publish -p:PublishProfile=publish -p:Password=${{ secrets.WEB_DEPLOY_PASSWORD }}
